{
    "name": "api",
    "version": "1.0.0",
    "description": "FastAPI Serverless API",
    "scripts": {
      "dev": "uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
      "build": "echo \"Building Python dependencies using uv lock...\" && uv lock --frozen",
      "start": "uvicorn main:app --host 0.0.0.0 --port 3000" 
    },
    "dependencies": {
    }
  }